# Object Oriented Programming — Lab Assignment 1

## Student Details

| Field              | Details              |
|--------------------|-----------------------|
| Name               | Tushar                |
| Enrollment Number  | 2026010130             |
| Program            | MCA 1st Year           |
| Subject            | Object Oriented Programming (OOPs) |
| Assignment         | Lab Assignment 1      |
| Topics Covered     | Basics of C++, Classes and Objects |

## Contents

This assignment covers the following programming questions:

1. Displaying "Hello World" and using control characters (`\n`, `\t`, `\a`, `\r`)
2. Celsius to Fahrenheit temperature conversion
3. Demonstrating `for`, `while`, and `do-while` loop variations
4. Creating a `Student` structure with data members and member functions
5. Differentiating `private` vs `public` access; implementing Question 4 using a class
6. Calling private member functions from within public member functions
7. Implementing a `Complex` class with private properties and public methods (`set()`, `display()`, `sum()`)
8. Demonstrating the use of `namespace` to avoid naming conflicts

---

# Object Oriented Programming — Lab Assignment 2

## Student Details

| Field              | Details              |
|--------------------|-----------------------|
| Name               | Tushar                |
| Enrollment Number  | 2026010130             |
| Program            | MCA 1st Year           |
| Subject            | Object Oriented Programming (OOPs) |
| Assignment         | Lab Assignment 2      |
| Topics Covered     | Constructors and Destructors |

## Contents

This assignment covers the following programming questions:

1. `Student` class with `name` and `rollNo`, initialised using a default constructor
2. `Rectangle` class with `length` and `breadth`, initialised using a parameterized constructor; displays area
3. `Employee` class with `name`, `id`, and `salary`, initialised using a parameterized constructor
4. `Box` class with `length`, `width`, and `height` — default constructor, parameterized constructor, and volume calculation
5. Constructor overloading using a `Student` class (default; name + roll number; name + roll number + CGPA)
6. `Complex` class with `real` and `imaginary` members — default constructor, parameterized constructor, and display function
7. `Demo` class demonstrating constructor/destructor call messages across multiple objects
8. Demonstrating execution order of constructors and destructors for objects created in `main()` vs. inside a separate function
